namespace Ged.Core.IO.Tex;

/// <summary>Identifies which on-disk texture format a <see cref="DecodedTexture"/> came from.</summary>
public enum TextureFormatKind
{
    Tga,
    Vbm,
    Dds,
    Png,
    Jpeg,
    Atx,
}
